# Create a module area.py:

# area_circle(r)
# area_rectangle(l, w)

# Use it in another file and take input from user.
pi =3.14
def  area_circle(r):
    return (pi * r * r)
def area_rec(m,n):
    return (m * n)
