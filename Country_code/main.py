# 1. pydantic model is the used for automatic data validation where it will check against the user input and it will raise an error if the item is not found.
# from fastapi import FastAPI
# from pydantic import BaseModel
# app = FastAPI()
# class User(BaseModel):
#     name:str
#     age:int
# @app.post("/user")
# async def create_user(u:User):
#     return {"data":u,"message":"user created"}
# what is middle ware and implement a custom middle ware to og the request time stamp
# from collections.abc import async_generator





